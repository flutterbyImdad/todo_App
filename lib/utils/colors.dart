import 'package:flutter/material.dart';

class AppColors {
  static const Color taskBorder = Color(0xFF9E9E9E); // Define custom color for task border

  // Define primary, secondary, accent, and taskDone colors with 8-digit hexadecimal integers
  static const Color primary = Color(0xFF00A8E8);
  static const Color secondary = Color(0xFFFF7B00);
  static const Color accent = Color(0xFF202C59);
  static const Color taskDone = Color(0xFF65A34B);
}
